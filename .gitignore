# Node / Electron artifacts
# Без завершающего слэша НАМЕРЕННО: `node_modules/` совпадает только с
# каталогами, а в git-ворктри это обычно симлинк на node_modules основного
# чекаута (иначе Electron пришлось бы качать заново). Симлинк каталогом не
# является, повисал как untracked и попадал под `git add -A`.
node_modules
dist/
*.log
.DS_Store
Thumbs.db

# Build cache
.cache/
_electron/.cache/

# IDE / tools
.vscode/
.playwright-mcp/

# Claude Code: commit shared settings/hooks/agents/skills, ignore personal/local state
.claude/settings.local.json
.claude/sessions/
.claude/cache/
.claude/**/__pycache__/
.claude/**/*.pyc

# Test artifacts
titlebar-test.png

# Screenshots & visual checks
screenshots/
*.png
!build/icon.png

# Эталоны визуальной регрессии НЕ в репозитории: это 30 PNG на 8.6 МБ, и каждая
# визуальная правка добавляла бы новую порцию в историю навсегда. Снимаются
# локально одной командой, когда нужны:
#   npm run screenshot && npm run visual:baseline   # принять текущий вид
#   npm run visual:check                            # сверить после правок
# На CI-раннере они всё равно не годятся: сняты на macOS, ubuntu рисует иначе.
tests/visual-baseline/

# Playwright artifacts (trace/screenshots от упавших e2e)
test-results/
playwright-report/

# Visual-audit / metrics outputs (generated by scripts/visual-audit.js)
artifacts/

# Coverage reports (npm run coverage → c8)
coverage/

# Superpowers brainstorm sessions
.superpowers/

# Изолированные рабочие каталоги (git worktree) — сюда SDD уводит исполнение плана
.claude/worktrees/

# Разборы и аудиты — рабочий стол, а не продукт (глобальное правило)
analysis/
