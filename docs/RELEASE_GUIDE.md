# 📦 Руководство по релизам Timer Widget

Это руководство объясняет, как создавать релизы для Windows и macOS через GitHub Actions и вручную.

---

## 🚀 Автоматическая сборка через GitHub Actions

### Как это работает

В проекте настроен GitHub Actions workflow (`.github/workflows/build.yml`), который:
1. Автоматически собирает приложение для Windows и macOS
2. Создает релиз на GitHub
3. Загружает все файлы в релиз

### Способ 1: Создание релиза через Git Tag (Рекомендуется)

Это основной способ - создаем тег, пушим его, и GitHub Actions автоматически соберет релиз.

```bash
# 1. Убедитесь, что все изменения закоммичены
git status

# 2. Обновите версию в package.json (если нужно)
# Откройте package.json и измените "version": "1.1.0" на нужную

# 3. Создайте коммит с новой версией
git add package.json
git commit -m "Release v1.2.0"

# 4. Создайте тег
git tag v1.2.0

# 5. Запушьте коммит и тег
git push origin main
git push origin v1.2.0
```

**Что произойдет:**
- GitHub Actions автоматически запустится
- Соберет приложение на Windows и macOS
- Создаст релиз v1.2.0 на GitHub
- Загрузит все файлы в релиз

### Способ 2: Ручной запуск через GitHub UI

Если нужно пересобрать релиз или запустить сборку без создания тега:

1. Откройте https://github.com/YOUR_USERNAME/timer-widget/actions
2. Выберите workflow "Build & Release" слева
3. Нажмите "Run workflow" справа
4. Выберите ветку (обычно `main`)
5. Нажмите зеленую кнопку "Run workflow"

**Что произойдет:**
- Соберутся билды для Windows и macOS
- Файлы будут доступны во вкладке "Artifacts" этого workflow run
- НО релиз НЕ создастся (только при наличии тега)

### Где скачать собранные файлы

#### Если был создан тег (Способ 1):
1. Откройте https://github.com/YOUR_USERNAME/timer-widget/releases
2. Найдите свой релиз (например, v1.2.0)
3. В секции "Assets" будут файлы:
   - `TimerWidget-Setup-1.2.0.exe` - Установщик для Windows
   - `TimerWidget-Portable.exe` - Portable версия для Windows
   - `TimerWidget-1.2.0.dmg` - DMG образ для macOS
   - `TimerWidget-1.2.0-mac.zip` - ZIP архив для macOS

#### Если был ручной запуск (Способ 2):
1. Откройте https://github.com/YOUR_USERNAME/timer-widget/actions
2. Найдите свой workflow run
3. Прокрутите вниз до секции "Artifacts"
4. Скачайте:
   - `TimerWidget-win` - Содержит Windows билды
   - `TimerWidget-mac` - Содержит macOS билды

---

## 🛠 Локальная сборка (для тестирования)

### Windows

```bash
# Установите зависимости (один раз)
npm install

# Соберите для Windows
npm run build:win
```

Результат в папке `dist/`:
- `TimerWidget-Setup-1.1.0.exe` - Установщик
- `TimerWidget-Portable.exe` - Portable версия

### macOS

```bash
# Установите зависимости (один раз)
npm install

# Соберите для macOS
npm run build:mac
```

Результат в папке `dist/`:
- `TimerWidget-1.1.0.dmg` - DMG образ
- `TimerWidget-1.1.0-mac.zip` - ZIP архив

### Сборка для всех платформ (требует соответствующей ОС)

```bash
npm run build:all
```

**Важно:** Для сборки macOS билдов нужен Mac. Windows билды можно собрать на любой ОС.

---

## 📝 Создание релиза на GitHub вручную

Если хотите создать релиз вручную (например, для pre-release):

### 1. Соберите файлы локально

```bash
# На Windows
npm run build:win

# На Mac
npm run build:mac
```

### 2. Создайте релиз на GitHub

1. Откройте https://github.com/YOUR_USERNAME/timer-widget/releases
2. Нажмите "Draft a new release"
3. Заполните форму:
   - **Tag version:** `v1.2.0` (новый тег или существующий)
   - **Target:** `main` (или другая ветка)
   - **Release title:** `Timer Widget v1.2.0`
   - **Description:** Опишите изменения в этом релизе
4. Перетащите собранные файлы из `dist/` в секцию "Attach binaries"
5. Опции:
   - ☑️ **Set as a pre-release** - если это бета-версия
   - ☑️ **Set as the latest release** - если это стабильная версия
6. Нажмите "Publish release"

---

## 🎯 Какие файлы включать в релиз

### Для Windows (обязательно оба):
- ✅ `TimerWidget-Setup-1.2.0.exe` - Основной установщик
- ✅ `TimerWidget-Portable.exe` - Portable версия (не требует установки)

### Для macOS (обязательно оба):
- ✅ `TimerWidget-1.2.0.dmg` - DMG образ (основной способ установки)
- ✅ `TimerWidget-1.2.0-mac.zip` - ZIP архив (альтернатива)

### Необязательные файлы:
- ❌ `*.yml` - Файлы для автообновлений (не нужны пока нет auto-updater)
- ❌ `*.blockmap` - Вспомогательные файлы (не нужны в релизе)

---

## 📊 Управление релизами на GitHub

### Как редактировать существующий релиз

1. Откройте https://github.com/YOUR_USERNAME/timer-widget/releases
2. Найдите нужный релиз
3. Нажмите "Edit release" справа
4. Внесите изменения:
   - Обновите описание
   - Добавьте/удалите файлы
   - Измените статус (pre-release/latest)
5. Нажмите "Update release"

### Как удалить релиз

1. Откройте релиз на GitHub
2. Нажмите "Delete" справа
3. Подтвердите удаление

**Важно:** Это удалит только релиз, но не тег! Чтобы удалить тег:

```bash
# Локально
git tag -d v1.2.0

# На GitHub
git push origin :refs/tags/v1.2.0
```

### Как пометить релиз как "Latest"

1. Откройте релиз
2. Нажмите "Edit release"
3. Снимите галочку "Set as a pre-release"
4. Поставьте галочку "Set as the latest release"
5. Нажмите "Update release"

---

## 🔍 Проверка сборки

После создания релиза проверьте:

### ✅ Checklist

- [ ] Релиз виден на странице https://github.com/YOUR_USERNAME/timer-widget/releases
- [ ] Есть все 4 файла (2 для Windows, 2 для macOS)
- [ ] Версия в названии файлов соответствует версии релиза
- [ ] Описание релиза понятное и содержит список изменений
- [ ] Релиз помечен как "Latest" (если это стабильная версия)
- [ ] Файлы скачиваются и запускаются

### 🧪 Тестирование билдов

#### Windows:
1. Скачайте `TimerWidget-Setup-X.X.X.exe`
2. Запустите установщик
3. Проверьте, что приложение работает
4. Скачайте `TimerWidget-Portable.exe`
5. Запустите без установки
6. Проверьте, что работает так же

#### macOS:
1. Скачайте `TimerWidget-X.X.X.dmg`
2. Откройте DMG
3. Перетащите приложение в Applications
4. Запустите и проверьте работу
5. При первом запуске macOS может спросить разрешение (System Preferences → Security)

---

## 🐛 Troubleshooting

### GitHub Actions не запускается

**Проблема:** После push тега ничего не происходит

**Решение:**
1. Проверьте, что тег начинается с `v` (например, `v1.2.0`)
2. Откройте Actions и проверьте, не отключены ли workflows
3. Проверьте, что файл `.github/workflows/build.yml` существует

### Сборка падает на macOS

**Проблема:** Build fails на macOS в GitHub Actions

**Решение:**
1. Проверьте логи в Actions
2. Обычно проблема в зависимостях - попробуйте обновить `electron` и `electron-builder`
3. Проверьте, что в `package.json` правильно указаны файлы в секции `build.files`

### Файлы не появляются в релизе

**Проблема:** Релиз создался, но файлов нет

**Решение:**
1. Откройте Actions → workflow run → Build step
2. Проверьте, что сборка прошла успешно
3. Проверьте логи "Upload artifacts" step
4. Возможно нужно подождать - релиз создается после завершения обеих сборок

### "Permission denied" при публикации релиза

**Проблема:** Workflow падает на этапе создания релиза

**Решение:**
1. Откройте Settings → Actions → General
2. В секции "Workflow permissions" выберите "Read and write permissions"
3. Сохраните и пересоздайте релиз

---

## 📚 Дополнительные ресурсы

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [electron-builder Documentation](https://www.electron.build/)
- [Creating Releases on GitHub](https://docs.github.com/en/repositories/releasing-projects-on-github/managing-releases-in-a-repository)
- [Git Tagging](https://git-scm.com/book/en/v2/Git-Basics-Tagging)

---

## 💡 Лучшие практики

### Версионирование (Semantic Versioning)

Используйте формат `MAJOR.MINOR.PATCH`:
- **MAJOR** (1.0.0 → 2.0.0) - несовместимые изменения API
- **MINOR** (1.0.0 → 1.1.0) - новая функциональность, обратная совместимость
- **PATCH** (1.0.0 → 1.0.1) - исправления багов

### Release Notes

Хорошее описание релиза должно содержать:

```markdown
## 🎉 Что нового

- Добавлена функция X
- Улучшена производительность Y

## 🐛 Исправлено

- Исправлен баг с Z
- Устранена проблема с W

## 🔧 Технические изменения

- Обновлен Electron до версии X
- Улучшена безопасность (CVE-XXXX-XXXX)
```

### График релизов

Рекомендуемый подход:
- **Патчи** (1.0.1, 1.0.2) - по мере необходимости (hotfixes)
- **Минор версии** (1.1.0, 1.2.0) - раз в 2-4 недели
- **Мажор версии** (2.0.0) - по мере готовности больших изменений

---

## 🎓 Пример полного процесса релиза

```bash
# 1. Завершите работу над фичами
git checkout main
git pull

# 2. Обновите версию
# Отредактируйте package.json: "version": "1.2.0"

# 3. Обновите changelog (если есть)
# Отредактируйте CHANGELOG.md

# 4. Закоммитьте изменения
git add package.json CHANGELOG.md
git commit -m "Release v1.2.0"

# 5. Создайте тег
git tag -a v1.2.0 -m "Version 1.2.0: Added new features and bug fixes"

# 6. Запушьте все
git push origin main
git push origin v1.2.0

# 7. Дождитесь завершения GitHub Actions
# Откройте https://github.com/YOUR_USERNAME/timer-widget/actions

# 8. Проверьте релиз
# Откройте https://github.com/YOUR_USERNAME/timer-widget/releases

# 9. Скачайте и протестируйте билды

# 10. Если все ОК - отметьте как "Latest release"
```

Готово! 🎊
