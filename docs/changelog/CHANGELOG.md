# 📝 Changelog — Timer Widget

Все значимые изменения в проекте документированы в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/).

---

## [1.2.4] — 2026-02-12

### Исправлено
- Глубокий аудит #2: устранено 12 дополнительных багов (BUG-A1..A7, DUP, DEAD, MAGIC)
- Утечка AudioContext в `beep()` — переиспользование контекста
- Мёртвый и недостижимый код удалён (14 мест)
- Magic numbers вынесены в `constants.js` (8 мест)
- Удалены console.log из продакшн-кода (4 шт.)
- Cleanup утечек памяти (5 мест)
- Несогласованность данных (3 места)
- CSS-улучшения (4 места)

### Изменено
- Обновлена документация и README

---

## [1.2.0] — 2026-01-22

### Добавлено
- Утилитарные модули: `security.js`, `utils.js`, `constants.js`
- Общие стили компонентов: `components.css`
- Unit-тесты: 3 файла, 7 тестов (100% pass)
- Валидация пользовательского ввода (security.js)
- Защита от XSS: `escapeHTML()` для user input
- CSP meta-тег во всех HTML-окнах
- Безопасная IPC-обёртка `ipc-compat.js`
- Горячие клавиши (Space в полноэкранном режиме)

### Изменено
- Security: `nodeIntegration: false`, `contextIsolation: true`, `sandbox: true` во всех окнах
- Оптимизация DOM: кэширование, skip-if-same для flip-карточек
- Резкое снижение энергопотребления (~60%)
- Синхронизация таймера через монотонный `timerUpdateCounter`
- `formatTime` делегирует в `formatTimeShort()` из `utils.js`
- Debounce 300 мс для resize events
- `localStorage` polling 100 мс → `storage` event + fallback 1 с

### Исправлено
- Все 43 бага исправлены (CRITICAL 3, HIGH 9, MEDIUM 13, LOW 3, PRIORITY 3, AUDIT 12)
- Memory Leak: IPC listeners — `cleanup()` + `beforeunload`
- Memory Leak: setInterval — `this.intervals[]` + `clearInterval()`
- Race Condition: `startTimer` — `timerLock` + `try/finally`
- Overtime limit: `<=` → `<`
- Progress при overtime: negative ratio обработан
- IPC error handling: `safelySendToWindow()` с `isDestroyed()` check

---

## [1.1.0] — 2025-12-09

### Добавлено
- Аналоговые мини-часы для info-блоков
- Кнопка закрытия
- Индикация overtime-статуса

### Исправлено
- Отключен auto-publish для electron-builder
- Отключена подпись кода
- Исправлены ошибки сборки

---

## [1.0.0] — Начальный релиз

### Добавлено
- Базовый функционал таймера с круговым прогресс-баром
- Мини-виджет таймера (always-on-top, прозрачный)
- Виджет часов (аналоговый/цифровой, дата, часовой пояс)
- Полноэкранный режим (4 стиля: Circle, Digital, Flip, Analog)
- Панель управления с вкладками
- 12 встроенных звуков + загрузка пользовательских
- 6 готовых тем оформления
- Фон: сплошной цвет, градиент, URL, локальный файл
- Режим переработки (overrun) с лимитом
- Быстрые пресеты времени
